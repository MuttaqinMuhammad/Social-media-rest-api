const OTP = require('../../models/OTP')

module.exports =(function(){
  
  setInterval(async()=>{

  /*

const otpList = await OTP.find()

const otpTimeLimit =otpSendTime + (5 * (1000 * 60))

if (otpReciveTime <= otpTimeLimit) {
	return true
} else {
	return false
}
  */
    
    
  }, 2000)
  
})()